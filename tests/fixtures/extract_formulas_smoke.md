## 43.0 Extract-formulas integration fixture

Heuristic codegen path requires a **parsed function** with **`formula: Some`** and a name/heuristic Match for subsidy helpers (**`GetBlockSubsidy`**).

**GetBlockSubsidy**: $h \rightarrow \mathbb{N}$

$$

\text{GetBlockSubsidy}(h) = 50 \cdot C

$$
