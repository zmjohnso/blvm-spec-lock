## 8.88 Golden standalone property

Round-trip extraction from a **Property** block (not tied to a **Function** header).

**Property** (Golden round-trip):

$$\text{OuterFunc}(\text{InnerFunc}(x)) = x$$
